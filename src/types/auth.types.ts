export interface SignupPayload {
  code: string
  name: string
  industry: string
  email: string
  phoneNumber: string
}

export interface MetaCredentials {
  wabaId: string
  phoneNumberId: string
  businessId: string
}
